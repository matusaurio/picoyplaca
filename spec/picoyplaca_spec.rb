require "spec_helper"

describe Picoyplaca do
  it "has a version number" do
    expect(Picoyplaca::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
